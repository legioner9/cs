using System;
using System.Dynamic;
using System.Xml;
using Microsoft.VisualBasic;
using System.Text.RegularExpressions;
namespace cs_cli
{
    class Program
    {
        static void Main()
        {

            double pi = Math.PI;
            Console.WriteLine(pi);

            Console.WriteLine("Math.Ceiling(2345.2345) = " + Math.Ceiling(2345.2345));
            Console.WriteLine("Math.Floor(2345.2345) = " + Math.Floor(2345.2345));
            Console.WriteLine("Math.Round(2345.51) = " + Math.Round(2345.51));
            Console.WriteLine("Math.Round(2345.5) = " + Math.Round(2345.5));

            Console.WriteLine("Math.Pow(5,3) = " + Math.Pow(5, 3));
            Console.WriteLine("Math.Max(5,3) = {0}" , Math.Max(5, 3));
                    
        }
    }
}