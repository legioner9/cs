using System;
using System.Dynamic;
using System.Collections.Generic;
using System.Xml;
using Microsoft.VisualBasic;
using System.Text.RegularExpressions;
using System.Globalization;
using System.Data;
namespace cs_cli
{
    class Program
    {
        static void Main()
        {
            string wrd_vlu = "Hello World)";
            PrnWL(wrd_vlu);
            PrnWL(wrd_vlu);
            SumWL(7, 4);
            //    PrnWL(SumRtn(10, 1));
            PrnWL(SumRtn(10, 1).ToString());
        }

        static void PrnWL(string wrd_arg)
        {
            Console.WriteLine(wrd_arg);
        }

        static void SumWL(int n1_arg, int n2_arg)
        {
            int res = n1_arg + n2_arg;
            Console.WriteLine("{0} + {1} = {2}", n1_arg, n2_arg, res);
        }

        static int SumRtn(int n1_arg, int n2_arg)
        {
            return n1_arg + n2_arg;
        }
    }
}
