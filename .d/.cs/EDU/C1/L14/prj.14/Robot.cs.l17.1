using System;
using System.Diagnostics.Metrics;
using System.Runtime.CompilerServices;

namespace prj
{
    class Robot
    {
        private static int count;
        private static string strLesson = "Robot obj has been created in les.15";

        public Robot(string name, int weight, byte[] coordinates)
        {
            System.Console.WriteLine(strLesson);
            this.name = name;
            this.weight = weight;
            this.coordinates = coordinates;
            count++;
        }
        public Robot()
        {
            System.Console.WriteLine(strLesson);
            count++;
        }

        private string name;
        private int weight;
        private byte[] coordinates;

        public int width;


        public int Weight
        {
            get
            {
                System.Console.WriteLine("weight = {0}", this.weight);
                return this.weight;
            }
            set
            {
                if (value < 1)
                {
                    System.Console.WriteLine("{0} < 1 set weight = 1", value);
                    this.weight = 1;
                }
                else
                {
                    System.Console.WriteLine("set weight = {0}", value);
                    this.weight = value;


                }

            }
        }

        public int Width
        {
            private get
            {
                return 0;
            }
            set
            {
                System.Console.WriteLine("set width = {0}", value);
                this.width = value;
            }
        }


    }
}