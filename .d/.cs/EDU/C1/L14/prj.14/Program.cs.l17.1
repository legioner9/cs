
using System;
using System.Reflection.Emit;


namespace prj
{
    class Program
    {
        static void Main()
        {
            // constr v1
            Robot rbt = new("1", 2, [3, 4, 5]);

            System.Console.WriteLine("Enter weight :");
            rbt.Weight = Convert.ToInt32(Console.ReadLine());
            System.Console.WriteLine(rbt.Weight);


            System.Console.WriteLine("Enter width :");
            rbt.Width = Convert.ToInt32(Console.ReadLine());
            // acsessor is privet - get usually 
            System.Console.WriteLine(rbt.width);

        }
    }
}
